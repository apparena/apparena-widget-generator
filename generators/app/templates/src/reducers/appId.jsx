import actionTypes from "../actions/types";

const defaultState = {};

export default function appIdReducer(state = defaultState, action = {}) {
    switch (action.type) {
        default:
            return state;
    }
}
